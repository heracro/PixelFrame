package com.pixelframe.model.eventListeners.eventListeners;

import android.view.View;

/**
 * This class implements event handler in which device search, connect (pair) with PicoFrame
 * via Bluetooth and sets connection context in a way later on (in SendButton) sending
 * bitmap and some additional info would be possible.
 */
public class ConnectButtonOnClickListener implements View.OnClickListener {

    public ConnectButtonOnClickListener() {

    }

    public void onClick(View v) {

    }
}
